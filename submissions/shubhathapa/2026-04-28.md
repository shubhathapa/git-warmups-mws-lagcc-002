## Questions to answer in your daily log

- What caused the conflict, in your own words?
because i changed the same line in the same file on two different branches.
- What do the `<<<`, `===`, `>>>` markers mean?
'<<<' is head of your current branch '=====' is seperator and '>>>>>' is incoming branch version
- How did you decide what to keep?
i removed the markers and merged the message.
- Was this scary or fine? Honest answer.
No

## Questions to answer in your daily log

- What are the sections of `git status` output? (modified, staged, untracked - what's each?) 
staged shows files that are ready to be committed
modified shows what changed but not added yet and untracked hows new files git hasnt tracked

- What format does each line of `git log --oneline` use?
the line shows the updated file and actions

- What's the graph in `--graph` actually drawing?
shows branch histroy visually, dots = commits, lines = branch paths, splits = branches diverging, merges = branches coming back together

- What does `git diff` show when there are no changes? What about when there are unstaged changes?
when there are no changes, it shows nothinh, if there are unstaged changes, it shows what was modified in your working diretory compared to the last commit